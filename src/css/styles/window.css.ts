import { style } from "@vanilla-extract/css";

export const windowContainer = style({
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
  width: '100%',
  overflow: 'hidden',
  position: 'relative',
});